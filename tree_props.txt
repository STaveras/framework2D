I've decided how I want to handle props. The trees should be processed like frames for a Character animation, where you give a frame size, and the number of frames, for that animation... But a little different. These Props can be multi-layered, parented images... For example, a tree, is a row in an Tree/<Color>-Tree.png. But, the source frames are staggered for the parts of the tree; so there's a row (y) of columns (x), and in our case, we have 12 columns; the tree is made from 6 parts, 