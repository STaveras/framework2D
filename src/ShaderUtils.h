// ShaderUtils.h

#include "FileSystem.h"
#include "Types.h"

namespace Shader
{
    static byte* LoadShader(const char* shaderPath) {
        
        // FileSystem::File file = FileSystem::OpenFile(shaderPath);
    }
}