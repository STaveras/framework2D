
#pragma once

#include "Types.h"
// #include "Material.h"

typedef struct vertex 
{
    vector3 position;
    vector3 color;

    // vertex(auto pos, Color color):position(pos[0],pos[1],pos[2], color(col) { }
}Vertex;